services:
  bot:
    build: .
    container_name: discord-music-bot
    restart: unless-stopped
    environment:
      - DISCORD_BOT_TOKEN=${DISCORD_BOT_TOKEN}
      - FFMPEG_PATH=/usr/bin/ffmpeg
    # No volume on /app – use files from the image so main.py is present